import React from "react";

const style = {
  textAlign: "center",
  fontWeight: "bold",
  marginTop: "50px"
};

const Footer = props => {
  return (
    <footer style={style}>
      <p>Copyright ©</p>
    </footer>
  );
};

export default Footer;
